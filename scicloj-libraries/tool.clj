;; idea -- pull down https://scicloj.github.io/docs/resources/model.edn and turn it into static HTML
;;
;; downsides -- early bound
;;
;; remedy -- datatables as a react component, so that I can load it when I want.
;;
;; Decision 2022-08-07 -- try react component for data tables instead, see how that works. It won't need any local updates to keep track.
